%div{:class=>"page-header"}
  %h1 All Groups

- @groups.each do |group| 
  %div{:class=>"group"}
    %h3= link_to group.group_name, group
    - if user_signed_in? 
      %span{:class=> "admin"}
        = link_to 'Delete', group, method: :delete, data: {confirm: 'Are you sure Delete this status?'}, class: "btn btn-danger btn-mini"