%p
  %b
    %h3= @group.group_name 

%h4 miembros

- @group.users.each do |user|
  %p
    = user.full_name

%div{:class =>"form-actions"} 
  = link_to 'Unirse', new_group_user_group_path(@group.id), class: "btn btn-primary"

%div
  = link_to 'Back', groups_path, class: "btn btn-primary btn-mini"